import {
    Input,
    Animate,
    Alert,
    Datepicker,
    Datatable,
    Datetimepicker,
    Modal,
    Ripple,
    Select,
    Collapse,
    Navbar,
    Carousel,
    initMDB
} from "./mdb.es.min.js";

//init
initMDB({Input});
initMDB({Animate});
initMDB({Alert});
initMDB({Datepicker});
initMDB({Datatable});
initMDB({Datetimepicker});
initMDB({Modal});
initMDB({Ripple});
initMDB({Select});
initMDB({Collapse});
initMDB({Navbar});
initMDB({Carousel});
